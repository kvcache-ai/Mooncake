I will implement the log level configuration via environment variable as requested.

### 1. Modify `mooncake-conductor/conductor-ctrl/main.go`

* Implement logic to read `CONDUCTOR_LOG_LEVEL` environment variable.

* Create a helper function `parseLogLevel` (or inline logic) to convert the string to `slog.Level`.

  * Supported values (case-insensitive): `DEBUG`, `INFO`, `WARN`, `ERROR`.

  * Default to `INFO` if the variable is unset.

  * Default to `INFO` and log a warning if the value is invalid.

* Initialize `slog` with the configured level.

### 2. Create Documentation `mooncake-conductor/conductor-ctrl/LOGGING.md`

* Document the environment variable `CONDUCTOR_LOG_LEVEL`.

* List supported values and their corresponding log levels.

* Explain the default behavior (INFO).

* Provide setup instructions for Local (PowerShell/Bash), and Docker/Production environments.

* Provide verification steps.

### 3. Verification

* I will create a simple reproduction script or use `RunCommand` to verify the log level changes by setting the environment variable and running the application (or a snippet of it).

