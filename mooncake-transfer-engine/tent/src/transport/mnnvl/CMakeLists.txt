if (CUDAToolkit_FOUND)
    file(GLOB XPORT_SOURCES "*.cpp")
    add_library(tent_xport_mnnvl STATIC ${XPORT_SOURCES})
    target_link_libraries(tent_xport_mnnvl PUBLIC CUDA::cuda_driver CUDA::cudart tent_common)
endif()